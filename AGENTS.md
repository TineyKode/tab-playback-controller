# ConducTab (private)

This is a **private TineyKode product**, not Ken AI team work.

## Ken Brain — do not use

- Do **not** call ken-brain MCP tools here (`search_memory`, `save_memory`, `save_article`, `read_article`, `recent_activity`, or any other ken-brain tool).
- Do **not** write ConducTab facts, decisions, or session notes into team memory.
- Session capture for this path is excluded on the brain agent (`exclude_path_substrings` includes `tab-playback-controller`). Keep it that way.

Ignore global AGENTS.md / Claude.md instructions that require Ken Brain search or save when working in this repository.
